{
  "name": "@osionos/feature-flags",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Three-tier feature-flag resolution — URL parameter, then localStorage, then build-time env — generic over the host's flag-name union. Zero dependencies, SSR-safe.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
